<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.apple.mybatis.mapper.ClazzMapper">

    <insert id="insert">
    </insert>
    <delete id="deleteById">
    </delete>
    <update id="update">
    </update>
    <select id="selectById">
    </select>

    <select id="selectAll">
    </select>

    <!-- ${}是先进行拼接，后进行编译   -->
    <delete id="deleteBatch">
        delete
        from xxxxx
        where id in ()
    </delete>

    <select id="selectAllByAscOrDesc">
    </select>

    <select id="selectByBrandLike">
    </select>
</mapper>